{
  "name": "centric-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-frontend": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "vercel-build": "npm run install-frontend && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0"
  }
}
